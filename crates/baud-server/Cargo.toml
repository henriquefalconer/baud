[package]
name = "baud-server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Local daemon for baud: orchestration, journal, reconstruction"

[[bin]]
name = "baud-server"
path = "src/main.rs"

[dependencies]
baud-proto = { path = "../baud-proto" }
baud-secret = { path = "../baud-secret" }
baud-keys = { path = "../baud-keys" }
baud-tape = { path = "../baud-tape" }
baud-tape-local = { path = "../baud-tape-local" }
baud-identity = { path = "../baud-identity" }
baud-init = { path = "../baud-init" }
baud-packages = { path = "../baud-packages" }
baud-driver = { path = "../baud-driver" }
baud-journal = { path = "../baud-journal" }
baud-tracing = { path = "../baud-tracing" }
baud-stream = { path = "../baud-stream" }
baud-multiverse = { path = "../baud-multiverse" }
baud-vcpu = { path = "../baud-vcpu" }
baud-snapshot = { path = "../baud-snapshot" }
baud-snapshot-store = { path = "../baud-snapshot-store" }
baud-raftlet = { path = "../baud-raftlet" }
baud-host = { path = "../baud-host" }
serde_yaml = { workspace = true }
axum = { workspace = true }
tokio = { workspace = true }
tower = { workspace = true }
tower-http = { workspace = true }
sqlx = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
uuid = { workspace = true }
blake3 = { workspace = true }
rand = { workspace = true }
rand_chacha = { workspace = true }
futures-util = { workspace = true }
base64 = "0.22"

[dev-dependencies]
tempfile = { workspace = true }
